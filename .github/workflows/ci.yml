name: CI

on:
  push:
    branches:
      - main
      - develop
      - feature/lint-workflow

  pull_request:
    branches:
      - main
      - develop

jobs:
  lint:
    name: Code Lint
    uses: ./.github/workflows/lint.yml
